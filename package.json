{
  "name": "tugas pesilat sequelize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node .",
    "migrate": "npx sequelize-cli db:migrate",
    "db:seed": "npx sequelize-cli db:seed:all",
    "migrate:fresh": "npx sequelize-cli db:migrate:undo:all && npm run migrate",
    "migrate:fresh --seed": "npx sequelize-cli db:migrate:undo:all && npm run migrate && npm run db:seed"
  },
  "keywords": [],
  "author": "Reza Khoirul Wijaya Putra",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "fastest-validator": "^1.15.0",
    "mysql2": "^2.3.3",
    "sequelize": "^6.25.8"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.2"
  }
}
